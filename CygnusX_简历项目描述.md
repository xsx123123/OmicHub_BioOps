# CygnusX · 简历项目描述

> 可直接粘贴到简历的项目条目。文末附版面紧张时的压缩版与面试问答提示。

## 完整版

- **`CygnusX`**：**可复现的组学分析多 `Agent` 交付基础设施（个人主导设计与开发）**

  **面向组学科研场景的四级 AI 产品体系，让每一次分析交付可审计、可验证、可复用**

  **技术栈**：`FastAPI` · `SQLAlchemy 2.0` · `PostgreSQL(pgvector)` · `Redis` · `MinIO` · `Celery/RocketMQ` · `Snakemake` · `LangGraph` · `Vue 3 + TypeScript` · `Docker Compose` · `OpenTelemetry`

  **项目亮点：**

  - **设计四级产品分层与成本闸门模型**：按 `token` 成本与交付约束将产品分为 `L1–L4` 级（`17+` 确定性生信工具零 `token` 消耗，`L4` 协作室仅在满足升级规则时刻意进入），从架构上隔离探索性分析与交付链路；
  - **以"虚拟生物信息部门"为组织模型设计多 `Agent` 协作室（`AgentTeams`）**：`Manager` 扮演**生信部门经理**（接单 / 派单 / 验收 / 交付 / 例外处理五职责），将任务分派给不同领域专家 `Agent`（RNA-seq / ATAC-seq / 单细胞 / QC / 交付报告等），质量审计独立核验、交付报告员只交付过质量门的产物；
  - **协作室双模式设计**：通用任务走**部门经理直接调度**的轻量模式（经理理解需求 → 分派专家 → 汇总答复）；正式分析走 **`Case` 受控交付模式**（创建 `Case` → 计划生成与审批 → 任务执行 → 质量审核 → 生成带 `Case / Artifact / Evidence / Manifest` 溯源证据链的可追溯交付物），按任务严肃程度选择协作强度；
  - **配置驱动的多 `Agent` 架构**：`19` 个内置专家 `Agent` 的身份 / 提示词 / 工具白名单 / 模型全部 YAML 声明、热重载；`43` 个 LLM 可调用工具统一注册，双通道返回（LLM 摘要 + 前端富渲染）；
  - **双执行引擎同语义设计**：`ReAct` 与 `LangGraph` 状态图共享 `ask_user` 中断、`handoff`、工具回灌语义；`6` 条执行路径共用统一生命周期事件契约，不重写 `Runtime` 即实现跨路径全链路可观测；
  - **`LLM` 规划治理**：领域包锚点硬约束 + 通用四阶段骨架兜底 + 能力匹配软校验与修复回路，解决幻觉式规划错派 `Agent` 问题；
  - **自研分层记忆系统**：`pgvector` + `HNSW`，共享 / `Agent` 双分区语义、时间衰减排序、幂等修正链、增量 settle 游标；
  - **`LLM` 过程全量记录子系统**：`MinIO` append-only 全文 + `PostgreSQL` 摘要索引 + `OTel` span 引用三级存储，best-effort 异步写不阻断主链，按角色做服务端字段裁剪；
  - **沙箱安全基线**：`cap_drop ALL` / 只读 rootfs / `seccomp` / 网络隔离 + egress 白名单 / 配额兜底等 `10+` 项容器控制，含基线漂移自动重建与结构化审计。

## 压缩版（版面紧张时使用）

- **`CygnusX`**：**可复现的组学分析多 `Agent` 交付基础设施（个人主导设计与开发）**

  技术栈：`FastAPI` · `PostgreSQL(pgvector)` · `Redis` · `MinIO` · `Snakemake` · `LangGraph` · `Vue 3` · `Docker`

  - **设计四级产品体系与成本闸门模型**：按 `token` 成本与交付约束分级（`L1–L4`），`17+` 确定性生信工具零 `token` 消耗，从架构上隔离探索性分析与交付链路；
  - **以"虚拟生物信息部门"为模型设计多 `Agent` 协作室**：`Manager` 作为生信部门经理统一接单派单，通用任务由经理直接调度 `19` 个领域专家 `Agent` 完成；正式分析创建 `Case` 走"计划审批 → 执行 → 质控 → 可追溯交付"受控流水线，交付物附带完整溯源证据链；
  - **双执行引擎同语义设计**：`ReAct` 与 `LangGraph` 共享中断 / 交接 / 工具回灌语义，`6` 条执行路径共用统一生命周期事件契约，实现跨路径全链路可观测；
  - **自研分层记忆系统与 `LLM` 规划治理**：`pgvector + HNSW` 双分区记忆（时间衰减、幂等修正链）；领域包锚点硬约束 + 能力软校验修复回路，杜绝幻觉式规划错派 `Agent`。

## 面试问答提示（不进简历，自用）

- **"部门经理调度"对应什么实现？** → `agentteams_manager_consultation` 执行路径；**Case 模式** → `Case / WorkItem / Evidence` 三层协作本体 + Bridge 状态机 + Gateway 仲裁 + 按角色最小权限令牌的 Worker 池。
- **多入口多 Runtime 如何统一观测？** → 不合并运行时，统一事件语义，按 `run_id / round / execution_path / tool_call_id` 关联。
- **Worker fan-out 如何防失控？** → 受限工具集、禁止递归派生、兄弟失败隔离、轮次 / 超时 Loop Guard。
- **过程记录会不会拖垮主链？** → best-effort 异步写，DB 查询绝不扫对象存储，失败仅告警。
- **记忆如何防污染？** → 共享 / Agent 双分区、内容哈希幂等、敏感凭据拦截、L4 专家零注入。
