# 环境

- 运行命令：`Rscript`（R >= 4.2）。
- R 包：`optparse`、`jsonlite`、`MutationalPatterns`、`NMF`、`ggplot2`、`patchwork`、`ggpubr`。
- 基因组包：`BSgenome.Hsapiens.UCSC.hg38`（`--genome hg38`）或 `BSgenome.Hsapiens.UCSC.hg19`（`--genome hg19`）。

脚本不下载依赖或参考数据。
