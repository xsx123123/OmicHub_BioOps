# Environment

- R: `optparse`, `jsonlite`, `tidyverse`, `ggplot2`.
- Input is a user-provided NUCmer `.delta` file; no external data are required.
- Rendering PDF requires the normal sandbox R graphics device; PNG uses `ggsave` at 300 DPI.
