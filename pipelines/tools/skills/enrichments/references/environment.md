# Environment

- R: `optparse`, `ontologyIndex`, `data.table`, `dplyr`, `stats`.
- Python 3 batch helper: `pandas`, `loguru`.
- The OBO and association data are external resources and must be provisioned before running. This package neither downloads nor embeds them.
