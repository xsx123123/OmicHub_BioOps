# Environment

- R: `optparse`, `jsonlite`, `data.table`, `dplyr`, `tidyr`, `tibble`, `stringr`.
- The count table is user-provided; no annotation/reference resource is downloaded or bundled.
- TPM output is a normalization for the supplied feature lengths and cannot correct gene-ID mismatches or upstream assignment bias.
