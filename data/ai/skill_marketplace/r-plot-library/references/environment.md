# 环境

- 运行命令：`Rscript`（R >= 4.2）。
- 通用依赖：`optparse`、`jsonlite`、`ggplot2`。
- Venn 依赖：`ggVennDiagram`、`patchwork`、`dplyr`。
- Volcano 依赖：`ggrepel`、`ggpubr`、`dplyr`。
- ATAC UpSet 依赖：`ggupset`、`ggpubr`、`tidyverse`。

不需要外部参考数据，也不进行网络下载。
