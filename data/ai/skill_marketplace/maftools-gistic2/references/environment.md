# Environment

- R: `optparse`, `jsonlite`, `data.table`, `dplyr`, `tidyr`.
- All GISTIC2 and clinical tables are user-provided. No MAF, reference genome or GISTIC2 installation is needed for these post-processing operations.
- The tool does not modify its inputs; it writes only below `--output`.
