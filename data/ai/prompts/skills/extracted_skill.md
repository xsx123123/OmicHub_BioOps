# {{name}}
{{description}}

## 使用说明
{{usage_instructions}}

## 参考实现
```{{language}}
{{source}}
```

## 来源与边界
该 Skill 由 OmicStudio 会话 `{{session_id}}` 的工作区脚本 `{{path}}` 提炼。脚本仅作为参考实现——提炼时的输入路径、资源限制、软件版本和数据权限属于原会话环境，直接复用会在新环境里静默出错，所以执行前重新检查这四项。
