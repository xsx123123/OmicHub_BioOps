# 分析中心双流程测试结果

**测试日期**：2026 年 7 月 11 日  
**测试范围**：分析中心 RNA-seq（RNAFlow）与 ATAC-seq（ATACFlow）流程  
**测试方式**：Snakemake 只读预演（`--dry-run`），不执行分析规则、不生成分析结果。

## 一、测试结论

两个流程均可成功完成 Snakemake 启动、外部项目配置加载、Schema 校验和参考基因组路径校验的前置阶段。

当前环境尚不具备启动真实分析任务的条件。两个流程均在**参考基因组资源校验**阶段停止，尚未进入样本表读取、输入 FASTQ 检查或 DAG 构建阶段。

| 流程 | 预演状态 | 当前结论 |
| --- | --- | --- |
| RNAFlow | 阻断 | 缺少配置所需的参考基因组资源；测试项目原始数据和样本表路径也未挂载。 |
| ATACFlow | 阻断 | ATACFlow 参考库根目录未挂载；测试项目原始数据和样本表路径也未挂载。 |

## 二、测试环境

| 项目 | 结果 |
| --- | --- |
| 工作目录 | `/home/zj/zj_code_libarary/OmicHub` |
| Snakemake 环境 | `/home/zj/miniconda3/envs/snakemake` |
| Snakemake 版本 | `9.17.3` |
| pandas 版本 | `2.3.3` |
| RNAFlow 版本要求 | `Snakemake >= 9.9.0`，满足 |
| ATACFlow 版本要求 | `Snakemake >= 9.9.0`，满足 |

说明：项目 `.venv` 中的 Snakemake 虽然版本为 `9.23.1`，但缺少 `pandas`，无法加载两个流程的样本处理模块。因此本次使用已安装且依赖完整的 Conda 环境进行预演。

## 三、RNAFlow 测试结果

### 执行命令

```bash
cd /home/zj/zj_code_libarary/OmicHub/pipelines/RNAFlow
/home/zj/miniconda3/envs/snakemake/bin/snakemake \
  --dry-run --cores 1 --nolock \
  --config analysisyaml=/home/zj/zj_code_libarary/OmicHub/pipelines/RNAFlow/config.yaml
```

### 已通过项

- 成功读取 RNAFlow 的默认配置与项目配置。
- 成功初始化流程日志与配置 Schema 校验。
- 成功加载规则文件前的参考资源校验逻辑。

### 阻断项

1. 配置指定的原始数据与元数据项目未挂载：
   - `/data/jzhang/project/Temp/PRJNA1224991_lettcue/00.raw_data`
   - `/data/jzhang/project/Temp/PRJNA1224991_lettcue/01.workflow/samples.csv`
   - `/data/jzhang/project/Temp/PRJNA1224991_lettcue/01.workflow/contrasts.csv`
2. RNAFlow 配置选用 `Lsat_Salinas_v11`，但当前 `/home/zj/reference/RNAFlow_reference` 下仅发现 `hg38` 参考目录；流程校验报出 STAR 索引、FASTA、GTF、GFF、RSEM 索引等资源缺失。

## 四、ATACFlow 测试结果

### 执行命令

```bash
cd /home/zj/zj_code_libarary/OmicHub/pipelines/ATACFlow
/home/zj/miniconda3/envs/snakemake/bin/snakemake \
  --dry-run --cores 1 --nolock \
  --config analysisyaml=/home/zj/zj_code_libarary/OmicHub/pipelines/ATACFlow/config.yaml
```

### 已通过项

- 成功读取 ATACFlow 的默认配置与项目配置。
- 成功初始化流程日志与配置 Schema 校验。
- 成功加载参考基因组和物种校验逻辑。

### 阻断项

1. 配置指定的原始数据与元数据项目未挂载：
   - `/data/jzhang/project/Temp/atac_human_PRJNA427322/00.raw_data`
   - `/data/jzhang/project/Temp/atac_human_PRJNA427322/01.workflow/samples.csv`
   - `/data/jzhang/project/Temp/atac_human_PRJNA427322/01.workflow/contrasts.csv`
2. ATACFlow 配置依赖的参考库根目录 `/data/jzhang/reference/ATACFlow_reference` 当前不存在，导致 Bowtie2 索引、FASTA、GTF、GFF 等参考资源校验失败。

## 五、后续测试建议

1. 恢复或挂载两个测试项目的 `00.raw_data`、`samples.csv` 与 `contrasts.csv`。
2. 补齐 RNAFlow 使用的 `Lsat_Salinas_v11` 参考库，或将测试配置切换至当前已存在且可用的参考版本。
3. 挂载或配置 ATACFlow 的参考库根目录，并确认目标物种的 Bowtie2 索引与注释文件完整。
4. 环境就绪后，先以 `only_qc: true` 进行小规模真实运行，确认 FASTQ、质控、日志和任务监控链路。
5. QC 测试通过后，再分别进行完整 RNA-seq 与 ATAC-seq DAG 预演和正式分析测试。

## 六、工作区说明

- 本次仅执行预演，不运行任何分析规则。
- 测试过程中产生的 ATACFlow Python 缓存改动已清理。
- 未修改 RNAFlow、ATACFlow 的流程代码、项目配置或已有工作区改动。
