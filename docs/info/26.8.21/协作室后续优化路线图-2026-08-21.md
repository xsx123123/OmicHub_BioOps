# 协作室后续优化路线图

> 版本：v1.0 ｜ 日期：2026-08-21
> 定位：能力咨询修复（A/B/C）施工完成后的**全量待办收口清单**。按优先级分五批，每项注明来源文档与依赖，可直接按批派发。
> 当前基线：Claude Science 融合 W0–W4 已施工；能力咨询修复 A/B/C 已施工（单测 47+11 绿，E2E-17 未跑）；框架全景文档自列 11 项 + 评审新增 N1–N9 大部分未动。
>
> **执行状态（2026-08-21 收口）**：批 0/1/3 全部完成；批 2 完成 3/4（2.4 等 LLM 凭证）；批 4 代码项全部完成（4.4 运维项待发布窗口，4.5 correlation_id 与 4.6 扩展项显式留待后续）。逐项状态、commit SHA 与验证证据见《协作室后续优化路线图-执行总结-2026-08-21.md》。

---

## 第 0 批：闭环手头批次（立即，1 天内）

| # | 事项 | 执行者 | 来源 |
|---|---|---|---|
| 0.1 | **E2E-17 staging 实测**：起真实平台栈，按《能力咨询修复计划》Part 7 手测 V1–V5——咨询无「已接收/规划阶段」字样、能力回答对照目录无「质量管控」越权、hi 不出澄清卡、执行类需求照常出立项确认卡、审计零 proposal 事件 | 用户手测 + kimi code 起栈 | 《协作室能力咨询误入接单流程-调研核对与修复实施计划》Part 7 |
| 0.2 | **目录源抽查**：staging 上抽一个 `ability_catalog_query` detail 输出，与 `GET /capabilities`（registry snapshot）对照口径一致性 | kimi code | 同上 Part 8.3 |
| 0.3 | **本批改动分组提交 + 记 SHA**：能力咨询修复全部改动落 commit，CI 状态留证——四档制第一条，防"修复未提交"重演 | kimi code | 同上 Part 8.6 |

---

## 第 1 批：N1–N6 地基核实（最高优先，提示词已就绪未执行）

**动作**：把《协作室框架全景文档评审结论-2026-08-21》**Part 5 的核实提示词**（只查不改）发给 kimi code。

| # | 核实项 | 为什么排最前 |
|---|---|---|
| N1 | 四档对账表（W0–W4 + 本批改动的 commit/CI/SHA） | 218 条未提交教训的立法；不核实则一切"已完成"无档可归 |
| N2 | D2 变更编排 / D3 Worker 治理是否施工；version_no 并发唯一性证据 | D3 缺位直接危及 F1 血缘登记的并发安全 |
| N3 | 422 拒绝后 Work Item 终态（重试上限/死信） | poison-pill 无限重试风险，修复可能自造故障面 |
| N4 | 自省端点 token 授权粒度 + SQL 绕过测试证据 | 跨房间数据面开口风险 |
| N5 | 取证链是否拼接 MinIO 事件流 + PG 判决/血缘表 | F1/F2 建的可信性在取证环节是否漏掉 |
| N6 | confirm-proposal 幂等补验状态 | 双线任务书遗留 |

> 报告回来后由设计评审侧按《评审结论》checklist 核对，推翻预判则就地留修正记录。

---

## 第 2 批：P0 修复（第 1 批报告回来后按证据动工）

| # | 事项 | 现状依据 |
|---|---|---|
| 2.1 | **路由层静默回落加代码硬约束**（P0-1）：路由无命中强制走澄清卡或 LLM 会诊，禁止直达 agent-code 兜底 | 证据已补齐（能力咨询调研 Q1 附带发现：`agentteams_room_response_service.py:859-866,900`、`agentteams_service.py:76`）；F9 实测 12/30 样本静默回落 F1=0 |
| 2.2 | **迁移双盲消除**（P0-2 + P1-6 合并）：修 `check_migrations.py`（兼容裸 `revision = ...` 写法）→ 3 个 alembic 迁移 staging 实跑 upgrade + 冒烟 | 校验工具坏 + 迁移未实跑 = 部署日迁移风险双盲 |
| 2.3 | **测试存量 quarantine**（P1-5 升级项）：64-65 失败 + 5 errors（含本批 9 个）打 quarantine 标记 → CI 只卡新增失败先转绿 → burn-down 清单逐个清零 | CI 不绿则四档制"已验证"档无法建立 |
| 2.4 | **F9 B 臂真实运行**（P0-4）：配 `TEAMING_EVAL_LLM_*` 凭证，`--llm-live` 跑 3 reps，用真实 mean±stddev 决定组队策略权重；金标准 30 条建议双人复核标注 | stub 口径 F1=0.458±0.483 统计效力不足 |

---

## 第 3 批：P1 加固

| # | 事项 | 备注 |
|---|---|---|
| 3.1 | **魔数转实测**（P1-7）：`omic_task_stall_timeout_seconds=900` 最先（看门狗正确性存疑，E2E-1 观测 17min+ 卡死未终止——若属实实为 P0 级正确性问题）；其次 `worker_heartbeat_ttl_seconds=180`、`planning/execution_timeout=120/600` | 50 项"待测"魔数的优先三项 |
| 3.2 | **F4 自省端点 ASGI 集成测试**（P1-8）：与 N4 核实结果合并施工——授权粒度、SQL 绕过用例（UNION/子查询/注释/别名）进集成测试 | 端点目前只有路由注册 + 服务层单测 |

---

## 第 4 批：体验深化与收尾

| # | 事项 | 来源 |
|---|---|---|
| 4.1 | 前端消费血缘清单（`artifact_version_lineage`）与 qc 判决分布展示位（P2-9） | 全景文档 §7 |
| 4.2 | Manager working_style 加"查历史先用 case_facts_query"显式引导（P2-10） | 同上 |
| 4.3 | F5 token 口径补齐 ROUTER_SYSTEM_PROMPT 模板（P2-11） | 同上 |
| 4.4 | **上线收尾线**：部署演练、MinIO 迁移对账、`check_custom_agents.py`、监控接线、摘 L4 标签 | 此前四批路线图 |
| 4.5 | **治理存量**：房间 GC 实施、correlation_id 写入点、Bridge 硬校验（subagents_spawnable 入注册表已随能力咨询修复顺手完成 ✓） | 同上 |
| 4.6 | **扩展项**：L2→L4 Phase D LLM 兜底、provenance 三层标注 | 同上 |

---

## 路线一句话

**先闭环手头的（E2E-17 + 提交）→ 再摸清地基（N1–N6）→ 按证据修 P0 → P1 加固 → 体验与收尾。**

依赖关系：第 2 批 2.2/2.3 不依赖 N1–N6 结果，可与第 1 批并行；2.1 可直接施工（证据已齐），不必等 N 报告。

---

## 附：文档索引

- 《协作室能力咨询误入接单流程-调研核对与修复实施计划》——本批修复的完整档案（调研/核对/施工/验收/施工核对）
- 《协作室框架全景文档评审结论-2026-08-21》——N1–N9 清单 + Part 5 核实提示词 + 11 项评级调整
- 《协作室融合ClaudeScience设计理念评估与实施方案》——F1–F9 落地 spec（W0–W4 已施工）
- 《协作室双线施工任务书-三件套实现与地基e2e验收》——D1–D3 与 E2E-1~11 原始 spec（D2/D3 状态待 N2 核实）
