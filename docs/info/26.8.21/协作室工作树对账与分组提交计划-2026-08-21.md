# 协作室工作树对账与分组提交计划

> 日期：2026-08-21
> 依据：《协作室L4审查修复与优化实施手册》阶段 1 任务 1
> 状态：对账表已输出，**提交动作待用户确认后执行**（夹带项归属需用户决策；hunk 级交叠文件需拆分提交）
> HEAD：`4680155`；未提交改动 218 条（`git diff --stat`：164 个已跟踪文件，+5527/−11975）

---

## 一、分组提交计划（顺序即手册口径：安全修复 → 地基修复 → 升级代码 → 夹带项）

| 组 | 内容 | 提交信息引用 | 前置测试 |
|---|---|---|---|
| G0 安全止血 | 阶段 0 四修复：B1 审批旁路（bridge/app.py、security.py、service.py 安全段）、B2 evidence 白名单与因果锚点（bridge/service.py、app.py）、B3 confirm_token 脱敏（bridge/service.py、主后端 agentteams_audit_events.py/agentteams_room_service.py/agentteams_audit_chain_service.py）、agent_stream 瞬态化（bridge/audit.py、room_mirror.py）+ `bridge/tests/test_security_hardening.py` | 审查报告 B1/B2/B3/D#2 | bridge 全量 + 主后端 agentteams 套件 |
| G1 地基修复 | 断点 A（agentteams_service.py limit clamp + 分页）、断点 F（case_store fail-fast，与 G2 交叠）、假登出（DefaultLayout.vue/authRefresh.ts）、Gateway 监控（gateway/app.py、admin 前端）、worker restart 策略、U-1 三环、租约盲区、错误结构化、前端错误分级 | 根因报告断点 A–F 编号 | 对应单测 |
| G2 升级代码 | MinIO 化（minio_store.py/migrate_to_minio.py/case_store.py/audit.py 持久化段/config.py/app.py healthz 段/pyproject/Dockerfile/deploy 接线 + test_minio_persistence/test_migrate_to_minio）、Case 解耦（迁移 t3u4v5w6x7y8/u4v5w6x7y8z9、agentteams_room_service、rooms API、bridge room namespace、前端 RoomProposalCard 等）、L2→L4（upgrade_advisor 等）、双注册表（capability_registry/chat_service/intent_router + 契约测试）、审计总线（audit_chain_service/fetch_case_audit_chain.py）、阶段 2 正确性加固（persist-then-commit/receipt/causation）、阶段 1 工程项（SHA 接线/CI/E2E-7） | 升级方案 Part 1/2/3 + 修复手册阶段 1/2 条目 | 各套件 |
| G3 夹带项 | 见第三节，**用户逐条决策后才提交** | — | — |

**hunk 级交叠文件**（G0/G1/G2 同文件，需 `git add -p` 或按 hunk 拆分，单独评审）：
`case_store.py`（断点 F + MinIO 化 + persist-then-commit）、`audit.py`（持久化 + 分级 + receipt + 瞬态化）、`bridge/app.py`（healthz + 安全端点 + room namespace）、`bridge/service.py`（安全段 + namespace + 租约盲区）、`config.py`（build_sha + MinIO 配置）、`agentteams_service.py`（断点 A + Case 解耦 + 错误结构化）、`authRefresh.ts`（假登出 + 错误分级）。

## 二、归属明细（(a) 地基手册修复 / (b) 08-21 升级与修复手册）

### (a) 地基手册阶段 0–3 对应改动

| 手册条目 | 涉及文件 |
|---|---|
| 阶段0-修复1（limit 契约错位，断点 A） | `agentteams_service.py`（`_BRIDGE_CASE_EVENTS_PAGE_LIMIT` clamp+游标分页）；`bridge/tests/test_bridge_contract.py` |
| 阶段0-修复2（case_store 静默丢 Case，断点 F） | `bridge/case_store.py`（fail-fast）、`bridge/app.py`（healthz `case_store_skipped_cases`）、`bridge/tests/test_failure_recovery.py` |
| 阶段0-修复3（假登出） | `frontend/src/layouts/DefaultLayout.vue`、`frontend/src/utils/authRefresh.ts` |
| 阶段1-问题1（Matrix Gateway 监控） | `frontend/src/api/admin/agentTeamsBridge.ts`、`frontend/src/components/admin/AgentTeamsBridgeTab.vue`、`gateway/cygnusx_agent_gateway/app.py` |
| 阶段1-问题2（worker 崩溃循环） | `deploy/docker/docker-compose.worker.yml`（`on-failure:5`） |
| 阶段1-问题3（/healthz git SHA） | `bridge/app.py`、`bridge/config.py`（字段）+ 本次补接线：bridge/Dockerfile、deploy/agentteams/docker-compose.agentteams.yml、主后端 `core/config.py`/`main.py`、deploy/docker/Dockerfile、deploy/docker/docker-compose.yml、Makefile、deploy/docker/README.md |
| 阶段2-修复1（U-1 三环） | `agent_consultation_service.py`、`bridge/service.py`（missing_fields/schema_summary）、`parallel_subagent_service.py` |
| 阶段2-修复2（租约盲区） | `bridge/service.py`（worker.lease_renewal_failed）、`worker/production_runner.py`、`worker/claim_next.py`、`worker/tests/test_production_runner.py` |
| 阶段2-修复3（错误结构化透传） | `core/exceptions.py`、`main.py`、`agentteams_service.py` |
| 阶段3-任务1（前端错误分级） | `frontend/src/utils/errorPresentation.ts(+spec)`、`authRefresh.ts`、`ProfileView.vue`、`agentTeamsStatus.ts`、`useAgentChatStream.ts` |
| 阶段3-任务2（e2e） | `tests/e2e/agentteams/test_default_bridge_e2e.py` + 本次 `test_hi_no_case_e2e.py`（E2E-7）、`tests/e2e/agentteams/README.md`、`.github/workflows/ci.yml` |

### (b) 08-21 升级 + 审查修复

- **MinIO 化**：`bridge/minio_store.py`、`migrate_to_minio.py`、`case_store.py`、`audit.py`、`config.py`、`app.py`、`pyproject.toml`、`Dockerfile`、`deploy/agentteams/docker-compose.agentteams.yml`、`bridge.env.example`、`deploy/docker/docker-compose.yml`（minio-init）+ `test_minio_persistence.py`、`test_migrate_to_minio.py`
- **Case 解耦**：`alembic/versions/t3u4v5w6x7y8_*`、`u4v5w6x7y8z9_*`、`agentteams_room_service.py`、`models/chat.py`（AgentTeamsRoomModel）、`api/v1/agentteams.py`（rooms 端点）、bridge `models.py`/`service.py`/`room_mirror.py`/`shared_state.py`（room namespace）、`celery tasks/agentteams.py`、`agentteams_room_response_service.py`、`agentteams_room_sync_service.py`、`project_service.py`、前端 `RoomProposalCard.vue`/`api/agentTeams.ts`/`utils/agentTeamsRoom.ts`/`AgentTeamsRoomView.vue`/`stores/agentTeamsPreferences.ts` + 测试若干
- **L2→L4**：`agentteams_upgrade_advisor.py`、`api/v1/chat.py`、`schemas/chat.py`、`agent_ability_catalog.py`、`data/ai/agent_ability.yaml` + `test_agentteams_upgrade_advisor.py`
- **双注册表**：`agentteams_capability_registry.py`、`chat_service.py`、`agentteams_intent_router.py` + `test_dual_registry_contract.py`
- **审计总线**：`agentteams_audit_events.py`、`agentteams_audit_chain_service.py`、`scripts/fetch_case_audit_chain.py`、`api/v1/agentteams.py`（audit-chain）+ `test_agentteams_audit_chain.py`
- **审查修复（阶段 0/2）**：bridge `app.py`/`security.py`/`service.py`/`audit.py`/`room_mirror.py`/`case_store.py`/`minio_store.py` + `test_security_hardening.py`；主后端 `agentteams_audit_events.py`/`agentteams_room_service.py`/`agentteams_audit_chain_service.py`/`agent_consultation_service.py`/`agentteams_room_response_service.py`/`agentteams_interruption_coordinator.py`

## 三、夹带项清单（需用户逐条决策归属）

1. **docs 大搬家**：`docs/info/26.8.18/` 整目录删除（约 30 个 md/png）、`docs/info/26.8.19/` 大量删除；新增未跟踪 `docs/info/26.8.20/`、`docs/info/26.8.21/`、26.8.19 两本新手册。→ 建议：单独一组"文档重组"提交。
2. **Manager 人格分层工作流**（对应 26.8.20 手册，非本次升级）：`data/ai/agentteams_manager.yaml`、`data/ai/prompts/agentteams_manager.md`、`data/ai/mas/agent_capabilities.yaml`、`data/CygnusX.yaml`、`core/config.py`（manager 校验）、`test_general_persona_isolation.py` + overdrive 状态文案（overdrive_*_service.py、celery tasks/overdrive.py、TaskProgressCard.vue 等）。
3. **技能市场/bio_skills 工作流**：`skills/bio_skills/`、`skills/SKILL_INSTALL_GUIDE.md`、`data/ai/skill_marketplace/bio-single-cell-*`（17 个目录）、`data/ai/scrna.yaml`、`data/ai/tools/research.yaml`/`cloud_ops.yaml`、`infrastructure/mcp/presets.py`。
4. **L4 文件系统融合手册工作流**：`agentteams_context_refs.py`、`agentteams_case_tool_service.py`、`agentteams_data_tool_service.py`、前端 `AskUserCard.vue`/`MentionMenu.vue`/`KimiChatInput.vue`（与 Case 解耦前端部分耦合，拆分时注意）。
5. **子模块脏**：`Protocol/FlowFrame`、`pipelines/{ATACFlow,RNAFlow,scrna,tools}`（gitlink 内脏改动，需进各子模块仓库单独处理）。
6. **其他小改**：`AgentSandbox.vue`（−85 纯删除）、`AgentWorkspace.vue`、`agentHub.ts`、`api/client.ts`、`api/chat.ts`、`Protocol/file_protocol.md`、`ARCHITECTURE_DESIN/` 新 md、`report/12_*`、其余伴随性测试更新。

## 四、提交前检查清单（执行提交时逐条过）

- [ ] 每组提交前跑对应测试套件（G0/G2：bridge 全量 + 主后端 agentteams 套件；G1：对应单测）
- [ ] hunk 交叠文件逐一评审拆分结果
- [ ] 提交信息引用断点编号/手册条目/升级 Part 编号
- [ ] 夹带项每组单独提交，信息注明来源工作流
- [ ] 全部提交后 `git status` 干净
