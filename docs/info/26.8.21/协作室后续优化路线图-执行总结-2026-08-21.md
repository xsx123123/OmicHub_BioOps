# 协作室后续优化路线图——执行总结（2026-08-21）

> 对象文档：《协作室后续优化路线图-2026-08-21.md》（五批待办收口清单）。
> 本文档记录各批执行状态、commit SHA、验证证据与遗留项。所有 commit 均未 push。

## 一、commit 总账（按时间序）

| SHA | 内容 | 对应批次 |
|---|---|---|
| 9dc8689 | 能力咨询修复改动分组提交 | 0.3 |
| e623d8b | E2E-17 V2 缺口修复（annotations 全链路/safe_only 放行/顶层 mcp_tools 激活）+ N1–N6 报告 | 0.1 / 批 1 |
| d988ac7 | check_migrations.py 兼容裸 revision 写法 | 2.2 |
| 774de50 | 路由静默回落硬约束（无命中强制澄清卡/LLM 会诊） | 2.1 |
| 103ec79 | 72 条存量失败打 quarantine + burn-down 清单 + 新建 .github/workflows/ci.yml | 2.3 |
| e994f3c | 看门狗接线修复（_scan_session 改走 refresh_case，E2E-1 卡死根因消除）+ 魔数清单更新 | 3.1 |
| 6d3dcb5 | F4 自省端点 ASGI 集成测试 10 条 + 修中间件白名单 401 真 bug + F4 垂直入库 | 3.2 |
| bb24724 | Manager/qc working_style 补 case_facts_query 引导 + F2 qc 判决契约垂直入库 | 4.2 |
| 050b891 | F5 token 完整预算口径（实测 4743 tokens）+ HEAD 4 个 ruff 修复 + F5 垂直入库 | 4.3 |
| 8696ffe | Bridge：F1 产物血缘/F4 自省客户端/BUG-E2E-03 配套入库 | 4.5 配套 |
| 2c5a770 | Bridge：房间命名空间归档（口径 §2 归档≠删除，冷存迁移 + 409 只读拦截） | 4.5 |
| 0700705 | 目录源抽查报告（agent-qc 抽样，两源口径一致） | 0.2 |
| 5661cf5 | 前端血缘清单 + qc 判决分布展示位（含用户端点 GET /cases/{id}/artifact-lineage） | 4.1 |

## 二、分批状态

### 第 0 批：闭环手头批次 ✅ 完成

- 0.1 E2E-17 staging 实测：首轮 V2 失败 → 三段根因修复 → 重跑 V1–V5 全过（`evidence/e2e-2026-08-21/E2E-17/fix-verify.md`）。
- 0.2 目录源抽查：`evidence/e2e-2026-08-21/批02-目录源抽查-2026-08-21.md`——ability_catalog_query detail 与 registry snapshot 逐项对照，无漂移。
- 0.3 分组提交 + 记 SHA：9dc8689。

### 第 1 批：N1–N6 地基核实 ✅ 完成

报告随 e623d8b 落盘（`docs/info/26.8.21/` 下 N1–N6 核实报告）。

### 第 2 批：P0 修复 —— 3/4 完成，2.4 等凭证

- 2.1 ✅ 路由静默回落硬约束，实测双方向通过（774de50）。
- 2.2 ✅ check_migrations.py 修复（d988ac7）。
- 2.3 ✅ 72 条 quarantine + CI 只卡新增失败 + burn-down 清单（103ec79）。burn-down 逐条清零是持续工作，见 `协作室测试quarantine-burn-down-2026-08-21.md`。
- 2.4 ⏸ **挂起等用户**：`TEAMING_EVAL_LLM_BASE_URL/API_KEY/MODEL` 凭证未提供，F9 B 臂 `--llm-live` 无法运行。提供后即可执行。

### 第 3 批：P1 加固 ✅ 完成

- 3.1 ✅ 看门狗接线修复是实测发现的真 P0：reconcile 此前仅由用户打开 Case 视图触发，已改走 refresh_case；注入 DB 绑定实测 45s 内自发 3 次 reconcile（e994f3c）。
- 3.2 ✅ F4 集成测试 10 条，并修出端点从未可达的中间件 401 真 bug（6d3dcb5）。

### 第 4 批：体验深化与收尾 —— 代码项全部完成，运维项待窗口

- 4.1 ✅ 前端血缘清单 + qc 判决分布展示位（5661cf5，含新用户端点与 4 条 ASGI 集成测试）。
- 4.2 ✅ Manager/qc working_style case_facts_query 引导（bb24724），0.2 抽查确认线上生效。
- 4.3 ✅ F5 token 模板口径补齐（050b891）。
- 4.4 部分完成：`check_custom_agents.py` 已在运行栈实跑，exit=0（无自建 Agent，C2 风险不成立）。**待运维窗口**：部署演练、MinIO 迁移对账、监控接线、摘 L4 标签——均需真实发布环境，本地无法实质推进。
- 4.5 ✅ 房间 GC 实施 = Bridge 房间命名空间归档（2c5a770，9 用例）；Bridge 硬校验此前已完成 ✓。**correlation_id 写入点显式留待后续**：L4 审查报告 #4 自评「可接受的已知限制，提取口径已定义，跨服务 trace 属后续」，实施需跨服务 trace 设计，不在本路线图代码量内。
- 4.6 ⏭ 扩展项（L2→L4 LLM 兜底、provenance 三层标注）：按定义属后续迭代。

## 三、验证基线（最终状态）

- 主仓：`uv run pytest tests/unit -m "not quarantine"` → **1926 passed / 15 skipped / 72 deselected（quarantine）**。
- Bridge：`integrations/agentteams/bridge` 全量 → **187 passed / 2 skipped**。
- ruff：所有本次改动文件零错误（bridge 目录 10 个 ASYNC240/I001 为 HEAD 既有债务，050b891 基线对照证实非本次引入）。
- 前端：vitest 6 passed、vue-tsc 零错误、build 退出码 0。
- 集成测试：introspection 10 条 + artifact-lineage 4 条全过。

## 四、遗留与风险提示

1. **批 2.4 等凭证**：F9 B 臂真实运行需要 `TEAMING_EVAL_LLM_*`，提供后执行。
2. **主仓工作区仍有约 650 个 W0–W4 历史未提交改动**（data/ai 提示词、yaml、前端若干、worker 脚本等，hunk 级交叠）。N1 对账表已给出口径；**本次未盲目批量入库**——这些改动需要先按 N1 对账表逐组核验归属再分组提交，建议作为独立批次处理。
3. **L4 审查报告「必须排期」余项**：#2 room.agent_stream 持久流写放大 + room mirror 刷屏、#6 CaseStore 幽灵态窗口（MinIO 无 Redis 部署不自愈）——均不在本路线图五批清单内，但严重度为缺陷级，建议排入下一路线图。
4. **quarantine burn-down**：72 条已隔离，清零进度见 burn-down 文档。
5. 所有 commit 未 push；push 前需用户确认。
